# Maven / Spring Boot
target/

# IntelliJ
.idea/
*.iml

# VS Code
.vscode/

# Logs
*.log

# Banco H2
*.db
*.mv.db
data/

# Azure — segredos (nunca commitar). azure/variables.sh é versionado de
# propósito nesta entrega (só nomes de recursos, sem nenhuma senha).
azure/variables.local.sh
.env
.env.*
*.pem

# Diagrama/PDF — artefatos de build local, mantidos fora do git
docs/*.pdf.tmp
